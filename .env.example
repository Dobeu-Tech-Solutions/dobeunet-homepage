# MongoDB + Netlify Functions Configuration
# Copy this file to `.env` and populate values before running locally.

# ====== MongoDB Atlas ======
# Standard Mongo connection string including credentials and default database.
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/?retryWrites=true&w=majority

# Optional: explicitly declare the database/collection names used by Netlify functions.
MONGODB_DB_NAME=dobeu
MONGODB_LEADS_COLLECTION=leads

# ====== Notifications ======
# Optional webhook for sending Slack/Teams/Discord alerts when a lead is submitted.
LEAD_ALERT_WEBHOOK_URL=https://hooks.slack.com/services/xxxx/yyyy/zzzz

# ====== Runtime Notes ======
# - Never commit your real .env to git (already ignored).
# - Netlify UI: add each variable under Site settings → Environment variables.
# - Local dev: `npm run dev` automatically loads `.env`.
